openapi: 3.0.3
info:
  title: URL Shortener API
  version: 1.0.0
  description: API for creating and managing shortened URLs.
paths:
  /url:
    post:
      summary: register a short url
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - url
                - alias
              properties:
                url:
                  type: string
                  format: uri
                  example: "https://en.cppreference.com/w/cpp/container/unordered_set.html"
                alias:
                  type: string
                  example: "asdfmvk"
      responses:
        '200':
          description: Successfully created a url 
          content:
            application/json:
              schema:
                type: object
                required:
                  - status
                  - alias
                  - url
                properties:
                  status:
                    type: string
                    example: "OK"
                  alias:
                    type: string
                    example: "asdfmvk"
                  url:
                    type: string
                    format: uri
                    example: "https://en.cppreference.com/w/cpp/container/unordered_set.html"
  /url/{alias}:
    delete:
      summary: Delete a url with given alias
      parameters:
        - in: path
          name: alias
          required: true
          schema:
            type: string
          description: Short URL alias
      responses:
        '200':
          description: Delete call was successfull
          content:
            application/json:
              schema:
                type: object
                required:
                  - status
                  - rowsDeleted
                properties:
                  status:
                    type: string
                    example: "OK"
                  rowsDeleted:
                    type: integer
                    example: 1
  /{alias}:
    get:
      summary: Redirect by a short url
      parameters:
        - in: path
          name: alias
          required: true
          schema:
            type: string
          description: Short URL alias
      responses:
        '302':
          description: Redirect on a base URL
          headers:
            Location:
              description: Short URL
              schema:
                type: string
                format: uri
        '200':
          description: alias is not in storage
          content:
            application/json:
              schema:
                type: object
                required:
                  - status
                  - error
                properties:
                  status:
                    type: string
                    example: "Error"
                  error:
                    type: string
                    example: "url with such alias not found: asdfmvkdsf"
